# Cross-Media Consistency Audit

审计模式：`final`  
审计范围：Q1、Q2、Q3，冻结数字、方法与代码计划、论文、图表、交付表。

## 结果

**PASSED**

## 已核对项目

- 论文中的 15 个引用键均能在 `paper/refs.bib` 中找到，且没有未引用题录；`paper/9.参考文献.tex` 使用 `gbt7714-numerical` 数字制样式生成书目。
- 论文中的 `\\ref` 均有对应 `\\label`，无重复标签。
- 论文引用的图像文件均存在，Q1、Q2、Q3 交付表均存在。
- Q1、Q2、Q3 的正式方法、销售模式、管理配置、主要指标和论文数值与各自冻结数字及材料包一致。
- Q1–Q3 的符号、单位、约束名称与 `planning/symbol_table.md` 和方法详解一致。

## 结论

Q1 新增豆类地力价值敏感性实验已由人工决策 `q1_bean_land_value_claim_scope` 确认，冻结时间、claim 决策 ID、材料包和结果来源已对齐。Q1–Q3 的冻结数字、方法参数、符号单位、图表路径、引用键和交付文件均通过核对。

审计日期：2026-08-29
